# Máquina Kanban

Estados válidos: intake → manager_created → manager_validated → control_tower_provisioned → handoffs_persisted → bigwriter_delivered → launch_jobs_created → daily_jobs. Kora é a autoridade de todas as transições.

Editorial: planned → research → outline → writing → seo_review → ads_review → draft. `published` não pertence à rotina diária e exige gates humanos.
