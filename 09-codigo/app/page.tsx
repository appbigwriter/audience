'use client'
import { FormEvent, useState } from 'react'
export default function Home(){const [message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget); const data={name:String(form.get('name')??''),slug:String(form.get('slug')??''),niche:String(form.get('niche')??'')};const res=await fetch('/api/blogs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...data,language:'pt',voice:'neutro'})});const json=await res.json();setMessage(res.ok?`Blog criado: ${json.schemaName}`:`Bloqueado: ${json.error}`)}return <main><header><span className="eyebrow">FBR AGENCY / BLOGS</span><h1>Editorial control tower</h1><p>Crie blogs temáticos, valide o gestor e acompanhe drafts sem publicar por acidente</p></header><section className="grid"><article><h2>Novo blog</h2><form onSubmit={submit}><input name="name" placeholder="Nome do blog" required/><input name="slug" placeholder="slug_do_blog" required/><input name="niche" placeholder="Nicho" required/><button type="submit">Criar com gestor validado</button></form>{message&&<p>{message}</p>}</article><article><h2>Governança</h2><ul><li>Kora é dona do Kanban</li><li>3 jobs/dia por blog</li><li>Todo conteúdo termina em draft</li><li>Sergio aprova publicação e gasto</li></ul></article></section></main>}
